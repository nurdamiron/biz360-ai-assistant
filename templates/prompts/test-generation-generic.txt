// src/templates/prompts/test-generation-generic.txt
Сгенерируй модульные тесты для следующего кода:

ПУТЬ К ФАЙЛУ:
{{filePath}}

КОД:
```
{{fileContent}}
```

ФРЕЙМВОРК:
{{testFramework}}

ТИП ФАЙЛА:
{{fileType}}

РЕЗУЛЬТАТЫ ВАЛИДАЦИИ:
{{#if validationResults}}
{{#each validationResults.warnings}}
- Предупреждение: {{message}} (строка {{line}})
{{/each}}
{{#each validationResults.criticalErrors}}
- Ошибка: {{message}} (строка {{line}})
{{/each}}
{{else}}
Валидация не проводилась или результаты отсутствуют.
{{/if}}

ДОПОЛНИТЕЛЬНЫЙ КОНТЕКСТ:
{{#if dependentFiles}}
Зависимые файлы:
{{#each dependentFiles}}
{{path}}
```
{{content}}
```
{{/each}}
{{/if}}

{{#if existingTests}}
Примеры существующих тестов:
{{#each existingTests}}
{{path}}
```
{{content}}
```
{{/each}}
{{/if}}

ИНСТРУКЦИИ:
1. Сгенерируй набор тестов для этого кода, используя указанный тестовый фреймворк.
2. Адаптируй подход к тестированию под тип файла и язык программирования.
3. Покрой тестами все основные функции и классы.
4. Добавь тесты для различных сценариев использования, включая граничные случаи и обработку ошибок.
5. Используй моки и стабы для внешних зависимостей.
6. Структурируй тесты логично, группируя их по связанной функциональности.
7. Добавь комментарии, объясняющие сложные части тестов.

{{#if testFramework}}
Используй синтаксис и возможности фреймворка {{testFramework}}.
{{/if}}